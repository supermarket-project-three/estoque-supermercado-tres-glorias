
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Histórico - Três Glórias</title>
    
    <link rel="shortcut icon" href="<?php echo $this->basePath('img/favicon-tres-glorias.ico'); ?>" type="image/x-icon">
    <link rel="stylesheet" href="<?php echo $this->basePath('css/preset.css'); ?>">
    
    <link rel="stylesheet" href="<?php echo $this->basePath('css/cadastro-produtos.css'); ?>">

    <link rel="stylesheet" href="<?php echo $this->basePath('css/historico.css'); ?>">
    
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900&display=swap" rel="stylesheet">


</head>
<body>

    <header class="main-header">
        <div class="left-header">
            <h1>Histórico</h1>
        </div>
        <div class="right-header">
            <nav>
                <ul>
                    <li><a href="<?php echo $this->url('dashboard'); ?>">Inicio</a></li>
                    <li><a href="<?php echo $this->url('dashboard-produtos'); ?>">Produtos</a></li>
                    <li><a href="<?php echo $this->url('dashboard-usuarios'); ?>">Usuários</a></li>
                    <li><a href="<?php echo $this->url('dashboard-setores'); ?>">Setores</a></li>
                    <li><a href="<?php echo $this->url('logout'); ?>">Logout</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <div class="container container-pagina-principal">
        <main class="main-content">

            <section class="filtros-box">
                <form method="GET" action="<?php echo $this->url('historico'); ?>">
                    <div class="filtros-grid">
                        
                        <div class="campo" style="flex-grow: 2;">
                            <label>Produto:</label>
                            <input type="text" name="produto" placeholder="Nome ou Marca..." 
                                   value="<?php echo $this->escapeHtml($this->filtro['produto']); ?>">
                        </div>

                        <div class="campo">
                            <label>Tipo:</label>
                            <select name="tipo">
                                <option value="">Todos</option>
                                <option value="entrada" <?= ($this->filtro['tipo'] == 'entrada') ? 'selected' : '' ?>>Entrada</option>
                                <option value="saida" <?= ($this->filtro['tipo'] == 'saida') ? 'selected' : '' ?>>Saída</option>
                            </select>
                        </div>

                        <div class="campo">
                            <label>De:</label>
                            <input type="date" name="data_inicio" value="<?php echo $this->filtro['data_inicio']; ?>">
                        </div>

                        <div class="campo">
                            <label>Até:</label>
                            <input type="date" name="data_fim" value="<?php echo $this->filtro['data_fim']; ?>">
                        </div>

                        <div class="campo" style="flex-grow: 0;">
                            <button type="submit" class="buttonVerde">Filtrar</button>
                        </div>
                        
                        <?php if(array_filter($this->filtro)): ?>
                            <div class="campo" style="flex-grow: 0;">
                                <a href="<?php echo $this->url('historico'); ?>" class="btn-limpar" style="display:block; padding: 10px; text-align:center;">Limpar</a>
                            </div>
                        <?php endif; ?>

                    </div>
                </form>
            </section>

            <section class="list-users">
                <h2>Movimentações Registradas</h2>
                
                <table id="list">
                    <thead id="list-header">
                        <tr>
                            <th>Data / Hora</th>
                            <th>Tipo</th>
                            <th>Produto</th>
                            <th>Qtd.</th>
                            <th>Responsável</th>
                            <th class="obs">Observação</th>
                        </tr>
                    </thead>
                    <tbody id="list-body">
                        <?php if (count($this->movimentacoes) == 0): ?>
                            <tr><td colspan="6" style="text-align:center; padding: 20px;">Nenhuma movimentação encontrada.</td></tr>
                        <?php else: ?>
                            <?php foreach ($this->movimentacoes as $mov): 
                                $tipo = $mov->getTipo();
                                $classeBadge = 'badge-' . $tipo;
                                $sinal = ($tipo == 'entrada') ? '+' : '-';
                            ?>
                            <tr>
                                <td><?php echo $mov->getData()->format('d/m/Y H:i'); ?></td>
                                
                                <td>
                                    <span class="badge <?php echo $classeBadge; ?>">
                                        <?php echo ucfirst($tipo); ?>
                                    </span>
                                </td>
                                
                                <td>
                                    <strong><?php echo $this->escapeHtml($mov->getProduto()->getNome()); ?></strong><br>
                                    <small style="color:#666;"><?php echo $this->escapeHtml($mov->getProduto()->getMarca()); ?></small>
                                </td>
                                
                                <td style="font-weight:bold; font-size:1.1rem;">
                                    <?php echo $sinal . $mov->getQuantidade(); ?>
                                </td>
                                
                                <td><?php echo $this->escapeHtml($mov->getUsuario()->getNome()); ?></td>
                                
                                <td class="obs" style="font-style: italic; color: #555;">
                                    <?php echo $this->escapeHtml($mov->getObservacao()); ?>
                                </td>
                            </tr>
                            <?php endforeach; ?>
                        <?php endif; ?>
                    </tbody>
                </table>
            </section>

        </main>
        
        <footer style="margin-top: 50px;">
            <div class="titulo-footer">
                <div class="container-img"><img src="<?php echo $this->basePath('img/cadeado.png'); ?>" alt="Cadeado" width="25px"></div>
                <h2>Ambiente seguro</h2>
            </div>
            <div class="info-footer">
                <p>Copyright© Três Glórias 2025</p>
            </div>
        </footer>
    </div>

</body>
</html>